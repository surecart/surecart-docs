name: Sync OpenAPI Documentation

on:
  workflow_dispatch:
    inputs:
      trigger_source:
        description: 'Source that triggered the sync'
        default: 'manual'

jobs:
  trigger-mintlify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Triggered by ${{ inputs.trigger_source }}"